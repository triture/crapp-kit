package crapp.model;

typedef CrappModel = {
    var public_key:String;
    var private_key:String;
    var token:String;
    var rounds:Int;
    var server_port:Int;

    var crapp_log_host:String;
}
